<div class="main-contents-wrapper">
  <div class="main-contents">
    <h3 class="main-text">投稿されたレビュー</h3>
    <ul class="main-albums-list">
      <% @albums.each do |album| %>
        <li>
          <a href="#"><img src="<%= "/album_images/#{album.image_name}" %>"></a>
          <h5>「<%= album.title %>」</h5>
        </li>
      <% end %>
    </ul>
  </div>
</div>